LLM_BASE_URL=http://localhost:8080/v1
LLM_GATEWAY_KEY=replace-with-platform-virtual-key
# Model alias. Through the gateway (default) use the allow-listed alias
# 'qwen-local-instruct' (free local Qwen) or 'claude-haiku-4-5'. Raw Ollama
# names like 'qwen3:1.7b' work ONLY if LLM_BASE_URL points at Ollama directly.
LLM_MODEL=claude-haiku-4-5
# Generation params (optional). Defaults reproduce the original behavior.
LLM_TEMPERATURE=0.0
LLM_MAX_TOKENS=512
# Structured-output strategy: 'text' (default; prompt JSON + parse, portable) or
# 'native' (llm.with_structured_output(Invoice); needs provider support).
LLM_STRUCTURED_MODE=text
