LLM_BASE_URL=http://localhost:8094/v1
LLM_GATEWAY_KEY=replace-with-platform-virtual-key
# Model alias. Through the gateway (default) use the allow-listed alias
# 'qwen-local-coder' (free local Qwen coder) or 'claude-haiku'. Raw Ollama
# names like 'qwen3:1.7b' work ONLY if LLM_BASE_URL points at Ollama directly.
LLM_MODEL=qwen-local-coder
# Generation params (optional). Defaults reproduce current behavior.
# Lower temperature = more deterministic; raise max_tokens for longer output.
LLM_TEMPERATURE=0.0
LLM_MAX_TOKENS=1024
DEFAULT_LANGUAGE=python
# Self-check is OFF by default. Set to 1 to smoke-run generated python in a
# subprocess (only honoured when language==python). Leave 0 for the safe path.
RUN_CODE_CHECK=0
CODE_CHECK_TIMEOUT=10
